const char INDEX_PAGE[] PROGMEM = R"HTML(
<!doctype html><html lang="tr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Pansiyon Kontrol</title><style>
:root{--nav:#15233a;--blue:#2463eb;--bg:#f4f7fb;--ink:#172033;--muted:#66758e;--line:#e5eaf2;--good:#087443;--bad:#b42318}*{box-sizing:border-box}body{margin:0;font:15px system-ui;background:var(--bg);color:var(--ink)}.shell{display:grid;grid-template-columns:240px 1fr;min-height:100vh}.side{background:var(--nav);color:#d6e0f1;padding:24px 14px}.brand{font-size:20px;font-weight:800;color:#fff;padding:5px 10px 28px}.brand small{display:block;font-size:12px;color:#9eb0ca;font-weight:500;margin-top:5px}.nav{display:grid;gap:6px}.nav button{background:transparent;border:0;color:#c4d1e4;text-align:left;padding:12px 14px;border-radius:9px;font:inherit;cursor:pointer}.nav button.active,.nav button:hover{background:#294263;color:#fff}.main{padding:30px;max-width:1250px;width:100%;margin:auto}.top{display:flex;justify-content:space-between;align-items:center;margin-bottom:24px}.top h1{margin:0;font-size:27px}.top small{color:var(--muted)}.view{display:none}.view.active{display:block}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.card{background:#fff;border:1px solid var(--line);border-radius:15px;padding:20px;box-shadow:0 3px 14px #1720330a}.stat strong{font-size:32px;display:block;margin-top:7px}.stat.in strong{color:var(--good)}.stat.out strong{color:var(--bad)}.status{margin-top:18px;font-weight:700}.two{display:grid;grid-template-columns:1fr 1.2fr;gap:18px;margin-top:18px}.card h2{font-size:18px;margin:0 0 14px}.hint,.meta{color:var(--muted);font-size:13px}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:0 12px}label{display:block;margin-top:11px;font-size:13px;font-weight:650}input{margin-top:5px;width:100%;padding:10px;border:1px solid #cbd5e1;border-radius:8px;font:inherit}.check{display:flex;align-items:center;gap:8px}.check input{width:auto;margin:0}button.primary,a.primary{margin-top:14px;border:0;border-radius:8px;padding:10px 14px;background:var(--blue);color:#fff;font:inherit;font-weight:700;cursor:pointer;text-decoration:none;display:inline-block}.sub{border:0;background:#edf3ff;color:#1d4ed8;padding:9px 12px;border-radius:8px;font:inherit;font-weight:700;cursor:pointer}.toolbar{display:flex;justify-content:space-between;gap:12px;margin-bottom:12px}.toolbar input{max-width:280px;margin:0}.row{display:flex;justify-content:space-between;gap:16px;padding:13px 0;border-bottom:1px solid var(--line)}.row:last-child{border:0}.badge{font-size:12px;font-weight:750;padding:5px 9px;border-radius:999px;white-space:nowrap}.in{color:var(--good);background:#dcfae6}.out{color:var(--bad);background:#fee4e2}.permit{color:#7a5200;background:#fff0c9}.event{display:grid;grid-template-columns:145px 1fr auto;gap:12px;padding:13px 0;border-bottom:1px solid var(--line)}.event:last-child{border:0}.system{display:grid;grid-template-columns:repeat(2,1fr);gap:14px}.system div{padding:14px;border:1px solid var(--line);border-radius:10px}.system b{display:block;font-size:13px;color:var(--muted);margin-bottom:5px}@media(max-width:760px){.shell{display:block}.side{padding:12px}.brand{padding:4px 8px 12px}.nav{display:flex;overflow:auto}.nav button{white-space:nowrap}.main{padding:18px}.stats,.two{grid-template-columns:1fr}.grid{grid-template-columns:1fr}.event{grid-template-columns:1fr}.system{grid-template-columns:1fr}}
</style></head><body><div class="shell"><aside class="side"><div class="brand">Pansiyon Kontrol<small>Giriş · Çıkış Yönetimi</small></div><nav class="nav"><button class="active" data-view="home">Genel Bakış</button><button data-view="allstudents">Tüm Öğrenciler</button><button data-view="students">Öğrenciler & Kartlar</button><button data-view="events">Giriş-Çıkış Geçmişi</button><button data-view="system">Sistem Durumu</button></nav></aside><main class="main"><div class="top"><div><h1 id="title">Genel Bakış</h1><small id="clock">Bağlanıyor...</small></div><small id="live">● Canlı</small></div>
<section id="home" class="view active"><div class="stats"><div class="card stat in"><span>İçeride</span><strong id="inside">0</strong><small>Şu an pansiyonda</small></div><div class="card stat out"><span>Dışarıda</span><strong id="outside">0</strong><small>Giriş bekliyor</small></div><div class="card stat"><span>Toplam öğrenci</span><strong id="total">0</strong><small>Kayıtlı öğrenci</small></div></div><div class="two"><div class="card"><h2>Son işlem</h2><div id="last" class="status">Sistem hazır</div><div id="lastDetail" class="meta"></div></div><div class="card"><h2>Hızlı durum</h2><div id="quick"></div></div></div></section>
<section id="students" class="view"><div class="two"><div class="card"><h2>Öğrenci ve kart ekle</h2><p class="hint">Aynı öğrenci numarasını kullanarak ikinci veya üçüncü kartı ekleyebilirsin. İzin durumu öğrenci detayından ayarlanır.</p><button id="capture" class="sub" type="button">Kart okut</button> <span id="cardState" class="hint"></span><form id="student"><label>Kart UID<input id="uid" name="uid" readonly required></label><div class="grid"><label>Öğrenci no<input name="number" required></label><label>Ad soyad<input name="name" required></label><label>Okul<input name="school" required></label><label>Sınıf<input name="grade" required></label><label>Şube<input name="section" required></label><label>Oda<input name="room"></label></div><button class="primary">Kartı kaydet</button></form><div id="result" class="hint"></div></div><div class="card"><div class="toolbar"><h2>Öğrenci listesi</h2><div><button id="listAll" class="sub" type="button">Tümünü listele</button><input id="filter" placeholder="Ad, no, sınıf ara"></div></div><div id="studentList"></div></div></div></section>
<section id="allstudents" class="view"><div class="card"><div class="toolbar"><div><h2>Tüm öğrenciler</h2><p class="hint">Detay, geçmiş ve fotoğraf için öğrencinin üstüne bas.</p></div><div><button id="listInside" class="sub" type="button">İçerdekileri listele</button><button id="listOutside" class="sub" type="button">Dışardakileri listele</button><button id="listEveryone" class="sub" type="button">Hepsi</button><button id="toggleAllFilter" class="sub" type="button">Filtrele</button></div></div><input id="allFilter" placeholder="Ad, no, okul, sınıf veya şube ara" style="display:none;max-width:360px"><div id="allStudentList"></div></div></section>
<section id="events" class="view"><div class="card"><div class="toolbar"><h2>Son giriş-çıkışlar</h2><a class="primary" href="/api/events" download="giris-cikis.csv">CSV indir</a></div><div id="eventList"></div></div></section>
<section id="system" class="view"><div class="card"><h2>Sistem durumu</h2><div class="system"><div><b>IP adresi</b><span id="ip">-</span></div><div><b>SD kart</b><span id="sd">-</span></div><div><b>Okuma sistemi</b><span id="scanner">-</span></div><div><b>Web erişimi</b><span>Parola korumalı</span></div></div></div><div class="card"><h2>Yoklama kontrolü</h2><p class="hint">Bu ayarlar yalnızca yetkili panelden değiştirilebilir.</p><button id="systemOpen" class="primary" type="button">Sistemi aç</button><button id="systemClose" class="sub" type="button">Sistemi kilitle</button><label class="check"><input id="bootLocked" type="checkbox"> Cihaz açılışta kilitli başlasın</label><label class="check"><input id="animationEnabled" type="checkbox"> LCD karşılama animasyonu açık</label></div><div class="card"><h2>Admin kartı</h2><p class="hint">Bir yöntem seç: UID, kartın fiziksel kimliğini kullanır; kart ID ise karta güvenli kimlik yazar.</p><button id="adminCaptureUid" class="sub" type="button">UID ile tanımla</button><button id="adminCapture" class="sub" type="button">Kart ID ile tanımla</button><span id="adminState" class="hint"></span></div></section>
</main></div><script>
let students=[],activity=[];const titles={home:'Genel Bakış',allstudents:'Tüm Öğrenciler',students:'Öğrenciler & Kartlar',events:'Giriş-Çıkış Geçmişi',system:'Sistem Durumu'};function badge(s,p){return p?'<span class="badge permit">İzinli</span>':`<span class="badge ${s==='IN'?'in':'out'}">${s==='IN'?'İçeride':'Dışarıda'}</span>`}function show(v){document.querySelectorAll('.view').forEach(e=>e.classList.toggle('active',e.id===v));document.querySelectorAll('.nav button').forEach(e=>e.classList.toggle('active',e.dataset.view===v));title.textContent=titles[v]}document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>show(b.dataset.view));async function refresh(){try{let s=await fetch('/api/status').then(r=>r.json()),d=await fetch('/api/dashboard').then(r=>r.json()),a=await fetch('/api/activity').then(r=>r.json());students=d.students;activity=a.events;inside.textContent=d.inside;outside.textContent=d.outside;total.textContent=students.length;clock.textContent=s.date+' · '+s.ip;last.textContent=s.message;lastDetail.textContent=s.student?'Son öğrenci: '+s.student:'';ip.textContent=s.ip;sd.textContent=s.sdReady?'Hazır':'Hata';updated.textContent=s.date;quick.innerHTML=students.slice(0,5).map(x=>`<div class="row"><div><b>${x.name}</b><div class="meta">${x.grade}/${x.section} · ${x.updated||'İşlem yok'}</div></div>${badge(x.state,x.permitted)}</div>`).join('')||'<span class="hint">Henüz öğrenci eklenmedi.</span>';renderStudents();renderAllStudents();renderEvents()}catch(e){live.textContent='● Bağlantı yok'}}function renderStudents(){let q=filter.value.toLocaleLowerCase('tr');studentList.innerHTML=students.filter(x=>(x.name+x.number+x.school+x.grade+x.section).toLocaleLowerCase('tr').includes(q)).map(x=>`<div class="row"><div><b>${x.name}</b><div class="meta">${x.number} · ${x.school} · ${x.grade}/${x.section} · Oda ${x.room||'-'}</div></div>${badge(x.state,x.permitted)}</div>`).join('')||'<span class="hint">Eşleşen öğrenci yok.</span>'}function renderAllStudents(){let q=allFilter.value.toLocaleLowerCase('tr'),shown=students.filter(x=>(x.name+x.number+x.school+x.grade+x.section+x.room).toLocaleLowerCase('tr').includes(q));allStudentList.innerHTML=shown.map(x=>`<div class="row clickrow" data-all-number="${x.number}"><div><b>${x.name}</b><div class="meta">${x.number} · ${x.school} · ${x.grade}/${x.section} · Oda ${x.room||'-'}</div></div>${badge(x.state,x.permitted)}</div>`).join('')||'<span class="hint">Eşleşen öğrenci yok.</span>'}function renderEvents(){eventList.innerHTML=activity.map(e=>`<div class="event"><span class="meta">${e.time}</span><div><b>${e.name||e.number}</b><div class="meta">${e.number} · ${e.uid}</div></div>${badge(e.state,false)}</div>`).join('')||'<span class="hint">Henüz giriş-çıkış kaydı yok.</span>'}async function checkCard(){try{let c=await fetch('/api/card/pending').then(r=>r.json());cardState.textContent=c.waiting?' Kart okutulması bekleniyor...':'';if(c.uid){uid.value=c.uid;cardState.textContent=' Kart okundu.'}}catch(e){}}capture.onclick=async()=>{await fetch('/api/card/capture',{method:'POST'});uid.value='';cardState.textContent=' Kart okutulması bekleniyor...'};student.onsubmit=async e=>{e.preventDefault();let r=await fetch('/api/students',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(student)))}),d=await r.json();result.textContent=r.ok?'Kart eşleştirildi.':(d.error||'Kayıt başarısız.');if(r.ok){student.reset();uid.value='';refresh()}};filter.oninput=renderStudents;toggleAllFilter.onclick=()=>{let hidden=allFilter.style.display==='none';allFilter.style.display=hidden?'block':'none';toggleAllFilter.textContent=hidden?'Filtreyi kapat':'Filtrele';if(hidden)allFilter.focus();else{allFilter.value='';renderAllStudents()}};allFilter.oninput=renderAllStudents;refresh();checkCard();setInterval(()=>{refresh();checkCard()},3000);
</script><script>
async function adminPanel(){try{let s=await fetch('/api/status').then(r=>r.json());scanner.textContent=s.systemUnlocked?'Açık — öğrenci kartları okunuyor':'Kilitli — admin kartı gerekli';adminState.textContent=s.adminRegistered?' Admin kartı tanımlı.':' Henüz admin kartı tanımlanmadı.';let c=await fetch('/api/card/pending').then(r=>r.json());if(c.target==='admin')adminState.textContent=' Admin kartı okutulması bekleniyor...';if(c.target===''&&c.uid)adminState.textContent=' Admin kartı kaydedildi.'}catch(e){}}adminCapture.onclick=async()=>{await fetch('/api/admin/capture?mode=id',{method:'POST'});adminState.textContent=' Kart ID için admin kartı okutulması bekleniyor...'};adminCaptureUid.onclick=async()=>{await fetch('/api/admin/capture?mode=uid',{method:'POST'});adminState.textContent=' UID için admin kartı okutulması bekleniyor...'};setInterval(adminPanel,2000);adminPanel();
</script><script>
async function connectionLight(){try{let r=await fetch('/api/status');if(!r.ok)throw 0;await r.json();live.textContent='● Bağlı';live.style.color='#087443'}catch(e){live.textContent='● Bağlantı yok';live.style.color='#b42318'}}connectionLight();setInterval(connectionLight,1500);
</script><script>
async function systemUi(){try{let s=await fetch('/api/status').then(r=>r.json());bootLocked.checked=s.bootLocked;animationEnabled.checked=s.animationEnabled;systemOpen.disabled=s.systemUnlocked;systemClose.disabled=!s.systemUnlocked}catch(e){}}async function systemAction(action){await fetch('/api/system',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action})});systemUi();refresh()}systemOpen.onclick=()=>systemAction('unlock');systemClose.onclick=()=>systemAction('lock');bootLocked.onchange=async()=>{await fetch('/api/system',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({bootLocked:bootLocked.checked})});systemUi()};animationEnabled.onchange=async()=>{await fetch('/api/system',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({animationEnabled:animationEnabled.checked})});systemUi()};systemUi();setInterval(systemUi,2500);
</script><style>#live{display:none!important}.history{width:100%;border-collapse:collapse}.history th,.history td{padding:10px;text-align:left;border-bottom:1px solid #e5eaf2;font-size:13px}.history th{color:#66758e;background:#f8fafc}.clickrow{cursor:pointer}.clickrow:hover{background:#f7faff}</style><script>
document.getElementById('system').insertAdjacentHTML('beforeend',`<div class="card"><h2>Admin paneli parolası</h2><p class="hint">Varsayılan parola korunur; yalnızca değiştirmek istersen doldur.</p><form id="passwordForm"><label>Mevcut parola<input id="currentPassword" type="password" required></label><label>Yeni parola<input id="newPassword" type="password" minlength="8" required></label><button class="primary">Parolayı değiştir</button></form><div id="passwordResult" class="hint"></div></div>`);
function eventTable(rows,heading){eventList.innerHTML=`<h2>${heading}</h2><div style="overflow:auto"><table class="history"><thead><tr><th>Tarih / Saat</th><th>Öğrenci</th><th>Öğrenci No</th><th>Kart UID</th><th>İşlem</th></tr></thead><tbody>${rows.map(e=>`<tr><td>${e.date||e.time||'-'}${e.date?' '+e.time:''}</td><td>${e.name||'-'}</td><td>${e.number||'-'}</td><td>${e.uid||'-'}</td><td>${badge(e.state,false)}</td></tr>`).join('')}</tbody></table></div>`}
function studentDetail(d){let checked=d.permitted?'checked':'';eventList.innerHTML=`<button class="sub" id="backStudents">← Öğrenci listesine dön</button><h2 style="margin-top:16px">${d.name}</h2><p class="meta">${d.number} · ${d.school} · ${d.grade}/${d.section} · Oda ${d.room||'-'}</p><label class="check"><input id="studentPermitted" type="checkbox" ${checked}> İzinli öğrenci — içeride/dışarıda sayılarına dahil etme</label><div id="permissionResult" class="hint"></div><div style="overflow:auto;margin-top:16px"><table class="history"><thead><tr><th>Tarih / Saat</th><th>Kart UID</th><th>İşlem</th></tr></thead><tbody>${d.events.map(e=>`<tr><td>${e.date} ${e.time}</td><td>${e.uid}</td><td>${badge(e.state,false)}</td></tr>`).join('')||'<tr><td colspan="3" class="hint">Henüz giriş-çıkış kaydı yok.</td></tr>'}</tbody></table></div>`;backStudents.onclick=()=>show('students');studentPermitted.onchange=async()=>{let r=await fetch('/api/student/permitted',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({number:d.number,permitted:studentPermitted.checked})}),j=await r.json();permissionResult.textContent=r.ok?'İzin durumu kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok)refresh()}}
window.renderEvents=function(){eventTable(activity,'Son giriş-çıkışlar')};window.renderStudents=function(){let q=filter.value.toLocaleLowerCase('tr'),shown=students.filter(x=>(x.name+x.number+x.school+x.grade+x.section).toLocaleLowerCase('tr').includes(q));studentList.innerHTML=shown.map(x=>`<div class="row clickrow" data-number="${x.number}" title="Detayı görmek için tıkla"><div><b>${x.name}</b><div class="meta">${x.number} · ${x.school} · ${x.grade}/${x.section} · Oda ${x.room||'-'} · Detay için tıkla</div></div>${badge(x.state,x.permitted)}</div>`).join('')||'<span class="hint">Eşleşen öğrenci yok.</span>'};studentList.onclick=async e=>{let row=e.target.closest('[data-number]');if(!row)return;let d=await fetch('/api/student-events?number='+encodeURIComponent(row.dataset.number)).then(r=>r.json());show('events');studentDetail(d)};allStudentList.onclick=async e=>{let row=e.target.closest('[data-all-number]');if(!row)return;let d=await fetch('/api/student-events?number='+encodeURIComponent(row.dataset.allNumber)).then(r=>r.json());show('events');studentDetail(d)};passwordForm.onsubmit=async e=>{e.preventDefault();let r=await fetch('/api/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({current:currentPassword.value,next:newPassword.value})}),d=await r.json();passwordResult.textContent=r.ok?'Parola değiştirildi. Sayfayı yenileyip yeni parolayla giriş yapın.':(d.error||'Parola değiştirilemedi.');if(r.ok)passwordForm.reset()};
</script><script>
document.getElementById('system').insertAdjacentHTML('beforeend',`<div class="card"><h2 style="color:#b42318">Her şeyi sıfırla</h2><p class="hint">Öğrenciler, kartlar, giriş-çıkış geçmişi ve admin ayarları silinir. Wi-Fi bilgileri silinmez.</p><button id="resetAdminCapture" class="sub" type="button">1. Admin kartını okut</button><span id="resetCardState" class="hint"></span><label>Admin paneli parolası<input id="resetPassword" type="password" autocomplete="current-password"></label><label>Onay için SIFIRLA yaz<input id="resetConfirmation" autocomplete="off"></label><button id="resetAll" class="primary" type="button" style="background:#b42318">2. Her şeyi sil</button><div id="resetResult" class="hint"></div></div>`);
resetAdminCapture.onclick=async()=>{let r=await fetch('/api/reset-admin/capture',{method:'POST'}),d=await r.json();resetCardState.textContent=r.ok?' Admin kartı okutulması bekleniyor...':(d.error||'Başlatılamadı.')};
resetAll.onclick=async()=>{if(!confirm('Tüm öğrenci, kart ve geçmiş kayıtları silinecek. Devam edilsin mi?'))return;let r=await fetch('/api/reset-all',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirmation:resetConfirmation.value,password:resetPassword.value})}),d=await r.json();resetResult.textContent=r.ok?'Tüm kayıtlar silindi. Sayfa yenileniyor...':(d.error||'Sıfırlanamadı.');if(r.ok)setTimeout(()=>location.reload(),1200)};
setInterval(async()=>{try{let s=await fetch('/api/status').then(r=>r.json());if(s.resetAdminPending)resetCardState.textContent=' Admin kartı okutulması bekleniyor...';else if(s.resetAdminConfirmed)resetCardState.textContent=' Admin kart onaylandı. Parolayı yazıp sıfırlayabilirsin.'}catch(e){}},1500);
studentDetail=function(d){let checked=d.permitted?'checked':'',photo=d.photoAvailable?`<img src="/api/student/photo?number=${encodeURIComponent(d.number)}&v=${Date.now()}" style="width:120px;height:150px;object-fit:cover;border-radius:10px;border:1px solid #e5eaf2">`:'<div class="hint">Fotoğraf eklenmemiş.</div>';eventList.innerHTML=`<button class="sub" id="backStudents">← Öğrenci listesine dön</button><h2 style="margin-top:16px">${d.name}</h2><p class="meta">${d.number} · ${d.school} · ${d.grade}/${d.section} · Oda ${d.room||'-'}</p><div class="card" style="padding:14px;margin:12px 0"><b>Öğrenci fotoğrafı</b><div style="margin-top:9px">${photo}</div><label>Uzaktan fotoğraf yükle<input id="studentPhoto" type="file" accept="image/*"></label><div id="photoResult" class="hint"></div></div><label class="check"><input id="studentPermitted" type="checkbox" ${checked}> İzinli öğrenci — içeride/dışarıda sayılarına dahil etme</label><div id="permissionResult" class="hint"></div><div style="overflow:auto;margin-top:16px"><table class="history"><thead><tr><th>Tarih / Saat</th><th>Kart UID</th><th>İşlem</th></tr></thead><tbody>${d.events.map(e=>`<tr><td>${e.date} ${e.time}</td><td>${e.uid}</td><td>${badge(e.state,false)}</td></tr>`).join('')||'<tr><td colspan="3" class="hint">Henüz giriş-çıkış kaydı yok.</td></tr>'}</tbody></table></div>`;backStudents.onclick=()=>show('allstudents');studentPermitted.onchange=async()=>{let r=await fetch('/api/student/permitted',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({number:d.number,permitted:studentPermitted.checked})}),j=await r.json();permissionResult.textContent=r.ok?'İzin durumu kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok)refresh()};studentPhoto.onchange=async()=>{let file=studentPhoto.files[0];if(!file)return;photoResult.textContent='Fotoğraf küçültülüyor...';let img=new Image(),url=URL.createObjectURL(file);img.onload=async()=>{let scale=Math.min(1,320/Math.max(img.width,img.height)),c=document.createElement('canvas');c.width=Math.round(img.width*scale);c.height=Math.round(img.height*scale);c.getContext('2d').drawImage(img,0,0,c.width,c.height);let blob=await new Promise(ok=>c.toBlob(ok,'image/jpeg',.72));let form=new FormData();form.append('photo',blob,'photo.jpg');let r=await fetch('/api/student/photo?number='+encodeURIComponent(d.number),{method:'POST',body:form}),j=await r.json();photoResult.textContent=r.ok?'Fotoğraf kaydedildi.':(j.error||'Yüklenemedi.');if(r.ok){d.photoAvailable=true;studentDetail(d)};URL.revokeObjectURL(url)};img.onerror=()=>{photoResult.textContent='Fotoğraf okunamadı.';URL.revokeObjectURL(url)};img.src=url}};let allStudentMode='all';renderAllStudents=function(){let q=allFilter.value.toLocaleLowerCase('tr'),shown=students.filter(x=>(allStudentMode==='all'||(!x.permitted&&x.state===allStudentMode))&&(x.name+x.number+x.school+x.grade+x.section+x.room).toLocaleLowerCase('tr').includes(q));allStudentList.innerHTML=shown.map(x=>`<div class="row clickrow" data-all-number="${x.number}"><div><b>${x.name}</b><div class="meta">${x.number} · ${x.school} · ${x.grade}/${x.section} · Oda ${x.room||'-'}</div></div>${badge(x.state,x.permitted)}</div>`).join('')||'<span class="hint">Bu filtrede öğrenci yok.</span>'};listInside.onclick=()=>{allStudentMode='IN';renderAllStudents()};listOutside.onclick=()=>{allStudentMode='OUT';renderAllStudents()};listEveryone.onclick=()=>{allStudentMode='all';renderAllStudents()};listAll.onclick=()=>{allStudentMode='all';allFilter.value='';allFilter.style.display='none';toggleAllFilter.textContent='Filtrele';renderAllStudents();show('allstudents')};filter.oninput=window.renderStudents;renderStudents();renderEvents();</script></body></html>
<script>
const detailWithPhoto=studentDetail;studentDetail=function(d){detailWithPhoto(d);let h=eventList.querySelector('h2');h.insertAdjacentHTML('afterend',`<button id="editStudent" class="sub" type="button">Bilgileri düzenle</button><form id="editStudentForm" style="display:none"><p class="hint">Öğrenci no kartlar ve geçmiş kayıtlarıyla bağlı olduğu için değiştirilemez: <b>${d.number}</b></p><div class="grid"><label>Ad soyad<input id="editName" value="${d.name}" required></label><label>Okul<input id="editSchool" value="${d.school}" required></label><label>Sınıf<input id="editGrade" value="${d.grade}" required></label><label>Şube<input id="editSection" value="${d.section}" required></label><label>Oda<input id="editRoom" value="${d.room||''}"></label></div><button class="primary">Bilgileri kaydet</button><div id="editResult" class="hint"></div></form>`);editStudent.onclick=()=>{editStudentForm.style.display=editStudentForm.style.display==='none'?'block':'none'};editStudentForm.onsubmit=async e=>{e.preventDefault();let r=await fetch('/api/student/details',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({number:d.number,name:editName.value,school:editSchool.value,grade:editGrade.value,section:editSection.value,room:editRoom.value})}),j=await r.json();editResult.textContent=r.ok?'Bilgiler kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok){d.name=editName.value;d.school=editSchool.value;d.grade=editGrade.value;d.section=editSection.value;d.room=editRoom.value;refresh();setTimeout(()=>studentDetail(d),350)}}};
</script>
<script>
document.getElementById('system').insertAdjacentHTML('beforeend',`<div class="card"><h2>Saatli yoklama kilidi</h2><p class="hint">Bu aralığın dışında öğrenci kartları işlem yapmaz. Admin kartı her zaman çalışır.</p><label class="check"><input id="timeLockEnabled" type="checkbox"> Saat kilidini aç</label><div class="grid"><label>En erken çıkış<input id="earliestExitTime" type="time" value="06:00"></label><label>Pansiyona son giriş<input id="lastEntryTime" type="time" value="22:00"></label></div><button id="saveTimeLock" class="primary" type="button">Saatleri kaydet</button><div id="timeLockResult" class="hint"></div></div>`);
async function timeLockUi(){try{let s=await fetch('/api/status').then(r=>r.json());timeLockEnabled.checked=s.timeLockEnabled;earliestExitTime.value=s.earliestExitTime;lastEntryTime.value=s.lastEntryTime;timeLockResult.textContent=s.timeLockEnabled?(s.attendanceTimeLocked?'Şu an saat kilidi aktif.':'Saat kilidi açık, kart okutma zamanı uygun.'):'Saat kilidi kapalı.'}catch(e){}}saveTimeLock.onclick=async()=>{let r=await fetch('/api/system',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({timeLockEnabled:timeLockEnabled.checked,earliestExitTime:earliestExitTime.value,lastEntryTime:lastEntryTime.value})}),j=await r.json();timeLockResult.textContent=r.ok?'Saat ayarları kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok)timeLockUi()};timeLockUi();setInterval(timeLockUi,5000);
</script>
<script>
document.querySelector('.nav').insertAdjacentHTML('beforeend','<button data-view="late">Geç Kalanlar</button>');document.querySelector('.main').insertAdjacentHTML('beforeend','<section id="late" class="view"><div class="card"><h2>Geç kalanlar</h2><p id="lateInfo" class="hint"></p><div id="lateList"></div></div></section>');titles.late='Geç Kalanlar';document.querySelector('[data-view="late"]').onclick=()=>show('late');
document.getElementById('system').insertAdjacentHTML('beforeend',`<div class="card"><h2>Geç giriş takibi</h2><p class="hint">Son giriş saatinden sonra gelen öğrencileri kaydet veya kartlarını engelle. Son giriş saati yukarıdaki saat kilidi kartından ayarlanır.</p><label class="check"><input id="lateTrackingEnabled" type="checkbox"> Geç gelenleri takip et</label><label class="check"><input id="lateCardBlocked" type="checkbox"> Son girişten sonra kartı hiç okutma</label><button id="saveLateSettings" class="primary" type="button">Geç giriş ayarlarını kaydet</button><div id="lateSettingsResult" class="hint"></div></div>`);
async function refreshLate(){try{let d=await fetch('/api/late').then(r=>r.json());lateInfo.textContent=d.enabled?(d.blocked?'Son giriş '+d.lastEntryTime+' sonrası kartlar engellenir.':'Son giriş '+d.lastEntryTime+' sonrası gelenler listelenir.'):'Geç giriş takibi kapalı.';lateList.innerHTML=d.events.map(x=>`<div class="row"><div><b>${x.name||x.number}</b><div class="meta">${x.number} · ${x.date} ${x.time} · ${x.uid}</div></div><span class="badge out">Geç giriş</span></div>`).join('')||'<span class="hint">Geç giriş kaydı yok.</span>'}catch(e){}}async function lateSettingsUi(){try{let s=await fetch('/api/status').then(r=>r.json());lateTrackingEnabled.checked=s.lateTrackingEnabled;lateCardBlocked.checked=s.lateCardBlocked}catch(e){}}saveLateSettings.onclick=async()=>{let r=await fetch('/api/system',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({lateTrackingEnabled:lateTrackingEnabled.checked,lateCardBlocked:lateCardBlocked.checked})}),j=await r.json();lateSettingsResult.textContent=r.ok?'Geç giriş ayarları kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok){lateSettingsUi();refreshLate()}};lateSettingsUi();refreshLate();setInterval(refreshLate,5000);
</script>
<script>
document.querySelector('.nav').insertAdjacentHTML('beforeend','<button data-view="manage">Yönetim</button>');document.querySelector('.main').insertAdjacentHTML('beforeend',`<section id="manage" class="view"><div class="two"><div class="card"><h2>Manuel giriş / çıkış</h2><form id="manualForm"><label>Öğrenci no<input id="manualNumber" required></label><label>İşlem<select id="manualState"><option value="IN">Pansiyona giriş</option><option value="OUT">Pansiyondan çıkış</option></select></label><label>Sebep<input id="manualReason" required placeholder="Kartını unuttu"></label><button class="primary">İşlemi kaydet</button></form><div id="manualResult" class="hint"></div></div><div class="card"><h2>Günlük CSV yedeği</h2><p class="hint">Her kayıt tarihli CSV dosyasına otomatik eklenir.</p><label>Tarih<input id="backupDate" type="date"></label><a id="downloadBackup" class="primary">Yedeği indir</a></div></div><div class="two"><div class="card"><h2>Oda durumu</h2><div id="roomList"></div></div><div class="card"><h2>Sistem teşhisi</h2><div id="diagnosticList" class="hint"></div></div></div></section>`);titles.manage='Yönetim';document.querySelector('[data-view="manage"]').onclick=()=>show('manage');backupDate.value=new Date().toISOString().slice(0,10);downloadBackup.onclick=()=>downloadBackup.href='/api/backups?date='+backupDate.value;backupDate.onchange=downloadBackup.onclick;manualForm.onsubmit=async e=>{e.preventDefault();let r=await fetch('/api/manual-event',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({number:manualNumber.value,state:manualState.value,reason:manualReason.value})}),j=await r.json();manualResult.textContent=r.ok?'Manuel işlem kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok){manualForm.reset();refresh()}};async function managementUi(){try{let a=await fetch('/api/rooms').then(r=>r.json()),b=await fetch('/api/diagnostics').then(r=>r.json());roomList.innerHTML=a.rooms.map(x=>`<div class="row"><b>Oda ${x.room}</b><span>${x.inside} içeride · ${x.outside} dışarıda</span></div>`).join('')||'<span class="hint">Oda kaydı yok.</span>';diagnosticList.innerHTML=`SD: ${b.sdReady?'hazır':'hata'}<br>Wi‑Fi: ${b.wifiConnected?b.wifiRssi+' dBm':'bağlı değil'}<br>RFID: 0x${Number(b.rfidFirmware).toString(16).toUpperCase()} · LCD: hazır<br>Son durum: ${b.lastMessage}`;}catch(e){}}managementUi();setInterval(managementUi,7000);
</script>
<script>
const detailWithAdmin=studentDetail;studentDetail=function(d){detailWithAdmin(d);let h=eventList.querySelector('h2');let cards=(d.cards||[]).map(x=>`<div class="row"><code>${x}</code><button class="sub removeCard" data-uid="${x}">Kartı kaldır</button></div>`).join('')||'<span class="hint">Kart yok.</span>';h.insertAdjacentHTML('afterend',`<div class="card" style="padding:14px;margin:12px 0"><b>İzin tarih aralığı</b><p class="hint">Aktif süre boyunca öğrenci izinli sayılır.</p><div class="grid"><label>Başlangıç<input id="leaveStart" type="datetime-local"></label><label>Bitiş<input id="leaveEnd" type="datetime-local"></label></div><button id="saveLeave" class="primary">İzni kaydet</button><div id="leaveResult" class="hint">${d.leaveActive?'Şu an izinli: '+d.leaveUntil:''}</div></div><div class="card" style="padding:14px;margin:12px 0"><b>Tanımlı kartlar</b>${cards}</div>`);saveLeave.onclick=async()=>{let r=await fetch('/api/student/leave',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({number:d.number,start:leaveStart.value,end:leaveEnd.value})}),j=await r.json();leaveResult.textContent=r.ok?'İzin kaydedildi.':(j.error||'Kaydedilemedi.');if(r.ok)refresh()};eventList.querySelectorAll('.removeCard').forEach(b=>b.onclick=async()=>{let p=prompt('Kartı kaldırmak için admin paneli parolasını yaz:');if(!p)return;let r=await fetch('/api/card/remove',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({uid:b.dataset.uid,password:p})}),j=await r.json();if(!r.ok)alert(j.error||'Kaldırılamadı.');else{d.cards=d.cards.filter(x=>x!==b.dataset.uid);studentDetail(d)}})};
</script>
<script>
document.getElementById('system').insertAdjacentHTML('afterbegin','<div class="card"><h2>SD kart doluluğu</h2><div id="sdUsage" class="status">Kontrol ediliyor...</div><div style="height:10px;background:#edf1f7;border-radius:99px;margin-top:10px;overflow:hidden"><div id="sdBar" style="height:100%;width:0;background:#2463eb"></div></div></div>');async function sdUsageUi(){try{let s=await fetch('/api/status').then(r=>r.json());if(!s.sdReady){sdUsage.textContent='SD kart okunamıyor.';sdBar.style.width='0';return}let used=s.sdUsed||0,total=s.sdTotal||0,p=total?Math.round(used*100/total):0;sdUsage.textContent=`${(used/1048576).toFixed(1)} MB kullanıldı · ${(total/1048576).toFixed(1)} MB toplam · %${p} dolu`;sdBar.style.width=p+'%';sdBar.style.background=p>90?'#b42318':p>75?'#d97706':'#2463eb'}catch(e){}}sdUsageUi();setInterval(sdUsageUi,5000);
</script>
<script>
document.getElementById('students').insertAdjacentHTML('beforeend',`<div class="card"><h2>Kart / telefon tanımla</h2><p class="hint">Öğrenci numarası ve ad soyad ile mevcut öğrenciyi seç. Fiziksel admin kartı sistemi açtıktan sonra yükleme modunu başlat; kartı veya uygulamadaki telefonu okut.</p><div class="grid"><label>Öğrenci no<input id="credentialNumber"></label><label>Ad soyad<input id="credentialName"></label></div><button id="credentialCapture" class="primary" type="button">Kart / telefon yükleme modunu aç</button><div id="credentialResult" class="hint"></div></div>`);
credentialCapture.onclick=async()=>{let r=await fetch('/api/credential/capture',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({number:credentialNumber.value,name:credentialName.value})}),j=await r.json();credentialResult.textContent=r.ok?'Yukleme modu acildi. Kart veya yuklemeye hazir telefonu okut.':(j.error||'Yukleme baslatilamadi.')};
</script>
<script>
document.getElementById('system').insertAdjacentHTML('beforeend',`<div class="card"><h2>Wi-Fi ayarlari</h2><p class="hint">Kaydedince cihaz yeniden baslar. Yeni ağa baglanamazsa Pansiyon-Yoklama erisim noktasini acar.</p><label>Ag adi (SSID)<input id="wifiSsid" maxlength="32"></label><label>Wi-Fi parolasi<input id="wifiPassword" type="password" maxlength="63" autocomplete="new-password"></label><small class="hint">Sifresiz ag icin parola alanini bos birak.</small><button id="saveWifi" class="primary" type="button">Wi-Fi ayarlarini kaydet</button><div id="wifiResult" class="hint"></div></div>`);
async function wifiUi(){try{let s=await fetch('/api/status').then(r=>r.json());wifiSsid.value=s.wifiSsid||''}catch(e){}}saveWifi.onclick=async()=>{let r=await fetch('/api/wifi',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ssid:wifiSsid.value,password:wifiPassword.value})}),j=await r.json();wifiResult.textContent=r.ok?'Kaydedildi. Cihaz yeniden basliyor...':(j.error||'Kaydedilemedi.')};wifiUi();
</script>
)HTML";
